# the only uncommented line in this file should contain the irfanview version
# retrievable from
# curl -s http://www.irfanview.com/main_start_engl.htm | grep -oiE "current version.{0,5}[0-9.]{1,5}" | cut -d ' ' -f3
# version four point four two stopped letting downloads of the zip complete successfully, so reverted to this one for now
4.42
